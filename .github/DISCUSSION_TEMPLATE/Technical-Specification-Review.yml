title: Technical Specification Review
labels: ["General Introduction"]
body:
  - type: textarea
    id: summary
    attributes:
      label: Summary
      description: Provide a short (1-2 sentences) overview of the project, including its purpose and goals.
      render: bash
    validations:
      required: true
  - type: textarea
    id: scope
    attributes:
      label: Scope
      description: Define the scope of the project, including what is within scope and what is out of scope.
      render: bash
    validations:
      required: true
  - type: textarea
    id: data model
    attributes:
      label: Data Model
      description: Define any changes to data models, including any added or removed indeces or relations.
      render: bash
    validations:
      required: true
  - type: textarea
    id: user stories
    attributes:
      label: User Stories
      description: List the ways the user will interact with the project. e.g. "User will be able to edit their profile."
      render: bash
    validations:
      required: true
  - type: textarea
    id: implementation details
    attributes:
      label: Implementation Details
      description: Provide details around how the feature will be implemented, and justify any decisions made. For example, this may include stuff that looks like
      render: bash
            value: |
      * We are using server-side rendering via Next.js instead of client-side rendering because XYZ.
      * To get a user's information from Discord, we will use the provided code to generate an access token from Discord's OAuth API, and then use that access token to hit various endpoints to get user's profile and guild data.
    validations:
      required: true
  - type: textarea
    id: scope
    attributes:
      label: Scope
      description: Define the scope of the project, including what is within scope and what is out of scope.
      render: bash
    validations:
      required: true
  - type: textarea
    id: scope
    attributes:
      label: Scope
      description: Define the scope of the project, including what is within scope and what is out of scope.
      render: bash
    validations:
      required: true
    - type: textarea
    id: scope
    attributes:
      label: Scope
      description: Define the scope of the project, including what is within scope and what is out of scope.
      render: bash
    validations:
      required: true
    - type: textarea
    id: scope
    attributes:
      label: Scope
      description: Define the scope of the project, including what is within scope and what is out of scope.
      render: bash
    validations:
      required: true
  - type: textarea
    id: scope
    attributes:
      label: Scope
      description: Define the scope of the project, including what is within scope and what is out of scope.
      render: bash
    validations:
      required: true
  - type: textarea
    id: scope
    attributes:
      label: Scope
      description: Define the scope of the project, including what is within scope and what is out of scope.
      render: bash
    validations:
      required: true
  - type: markdown
    attributes:
      value: |
        ## Markdown header
        And some more markdown
  - type: input
    id: has-id
    attributes:
      label: Suggestions
      description: A description about suggestions to help you
    validations:
      required: true
  - type: dropdown
    id: download
    attributes:
      label: Which area of this project could be most improved?
      options:
        - Documentation
        - Pull request review time
        - Bug fix time
        - Release cadence
    validations:
      required: true
  - type: checkboxes
    attributes:
      label: Check that box!
      options:
        - label: This one!
          required: true
        - label: I won't stop you if you check this one, too
  - type: markdown
    attributes:
      value: |
        ### The thrilling conclusion
        _to our template_
